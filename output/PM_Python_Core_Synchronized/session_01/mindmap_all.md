---
markmap:
  colorFreezeLevel: 2
---
# Giới thiệu Python & Thiết lập môi trường

## Session 01: Giới thiệu ngôn ngữ Python
  ```markmap
  - Hiểu rõ khái niệm cốt lõi và kiến trúc của Giới thiệu ngôn ngữ Python.
  - Áp dụng các quy tắc triển khai thực tiễn để giải quyết bài toán: Cài đặt thành công môi trường.
  - Vận hành và kiểm tra đầu ra hoạt động ổn định.
  - Giải pháp kỹ thuật giúp tối ưu hóa hiệu năng, giảm thiểu blocking I/O và tự động hóa validation.
  - Ví dụ triển khai:
        student_name = "Nguyễn Văn A"  # Kiểu chuỗi (str)
        student_age = 20               # Kiểu số nguyên (int)
        student_gpa = 3.8              # Kiểu số thực (float)
        is_active = True               # Kiểu luận lý (bool)
        print("--- THÔNG TIN SINH VIÊN ---")
        print(f"Họ tên: {student_name}")
        print(f"Tuổi: {student_age}")
        print(f"GPA: {student_gpa}")
        extra_years = input("Nhập số năm cộng thêm: ")
        future_age = student_age + int(extra_years)
        print(f"Tuổi của sinh viên sau {extra_years} năm nữa là: {future_age}")
  - Tránh bỏ sót các tham số bắt buộc.
  - Cấu hình môi trường ảo venv chính xác và không đặt trùng tên file hệ thống.
  [Prompt: Generate a flowchart diagram showing standard input processed by control flow logic and printing the formatted output.]
  ```

## Session 01: Cài đặt môi trường
  ```markmap
  - Hiểu rõ khái niệm cốt lõi và kiến trúc của Cài đặt môi trường.
  - Áp dụng các quy tắc triển khai thực tiễn để giải quyết bài toán: Môi trường Python hoạt động.
  - Vận hành và kiểm tra đầu ra hoạt động ổn định.
  - Giải pháp kỹ thuật giúp tối ưu hóa hiệu năng, giảm thiểu blocking I/O và tự động hóa validation.
  - Ví dụ triển khai:
        student_name = "Nguyễn Văn A"  # Kiểu chuỗi (str)
        student_age = 20               # Kiểu số nguyên (int)
        student_gpa = 3.8              # Kiểu số thực (float)
        is_active = True               # Kiểu luận lý (bool)
        print("--- THÔNG TIN SINH VIÊN ---")
        print(f"Họ tên: {student_name}")
        print(f"Tuổi: {student_age}")
        print(f"GPA: {student_gpa}")
        extra_years = input("Nhập số năm cộng thêm: ")
        future_age = student_age + int(extra_years)
        print(f"Tuổi của sinh viên sau {extra_years} năm nữa là: {future_age}")
  - Tránh bỏ sót các tham số bắt buộc.
  - Cấu hình môi trường ảo venv chính xác và không đặt trùng tên file hệ thống.
  [Prompt: Generate a flowchart diagram showing standard input processed by control flow logic and printing the formatted output.]
  ```

## Session 01: Khai báo Biến
  ```markmap
  - Hiểu rõ khái niệm cốt lõi và kiến trúc của Khai báo Biến.
  - Áp dụng các quy tắc triển khai thực tiễn để giải quyết bài toán: Script khai báo biến cơ bản.
  - Vận hành và kiểm tra đầu ra hoạt động ổn định.
  - Giải pháp kỹ thuật giúp tối ưu hóa hiệu năng, giảm thiểu blocking I/O và tự động hóa validation.
  - Ví dụ triển khai:
        student_name = "Nguyễn Văn A"  # Kiểu chuỗi (str)
        student_age = 20               # Kiểu số nguyên (int)
        student_gpa = 3.8              # Kiểu số thực (float)
        is_active = True               # Kiểu luận lý (bool)
        print("--- THÔNG TIN SINH VIÊN ---")
        print(f"Họ tên: {student_name}")
        print(f"Tuổi: {student_age}")
        print(f"GPA: {student_gpa}")
        extra_years = input("Nhập số năm cộng thêm: ")
        future_age = student_age + int(extra_years)
        print(f"Tuổi của sinh viên sau {extra_years} năm nữa là: {future_age}")
  - Tránh bỏ sót các tham số bắt buộc.
  - Cấu hình môi trường ảo venv chính xác và không đặt trùng tên file hệ thống.
  [Prompt: Generate a flowchart diagram showing standard input processed by control flow logic and printing the formatted output.]
  ```

## Session 01: Kiểu dữ liệu cơ bản
  ```markmap
  - Hiểu rõ khái niệm cốt lõi và kiến trúc của Kiểu dữ liệu cơ bản.
  - Áp dụng các quy tắc triển khai thực tiễn để giải quyết bài toán: Hiểu các kiểu dữ liệu nền tảng.
  - Vận hành và kiểm tra đầu ra hoạt động ổn định.
  - Giải pháp kỹ thuật giúp tối ưu hóa hiệu năng, giảm thiểu blocking I/O và tự động hóa validation.
  - Ví dụ triển khai:
        student_name = "Nguyễn Văn A"  # Kiểu chuỗi (str)
        student_age = 20               # Kiểu số nguyên (int)
        student_gpa = 3.8              # Kiểu số thực (float)
        is_active = True               # Kiểu luận lý (bool)
        print("--- THÔNG TIN SINH VIÊN ---")
        print(f"Họ tên: {student_name}")
        print(f"Tuổi: {student_age}")
        print(f"GPA: {student_gpa}")
        extra_years = input("Nhập số năm cộng thêm: ")
        future_age = student_age + int(extra_years)
        print(f"Tuổi của sinh viên sau {extra_years} năm nữa là: {future_age}")
  - Tránh bỏ sót các tham số bắt buộc.
  - Cấu hình môi trường ảo venv chính xác và không đặt trùng tên file hệ thống.
  [Prompt: Generate a flowchart diagram showing standard input processed by control flow logic and printing the formatted output.]
  ```

## Session 01: Nhập, xuất dữ liệu trong python
  ```markmap
  - Hiểu rõ khái niệm cốt lõi và kiến trúc của Nhập, xuất dữ liệu trong python.
  - Áp dụng các quy tắc triển khai thực tiễn để giải quyết bài toán: Chương trình I/O cơ bản.
  - Vận hành và kiểm tra đầu ra hoạt động ổn định.
  - Giải pháp kỹ thuật giúp tối ưu hóa hiệu năng, giảm thiểu blocking I/O và tự động hóa validation.
  - Ví dụ triển khai:
        student_name = "Nguyễn Văn A"  # Kiểu chuỗi (str)
        student_age = 20               # Kiểu số nguyên (int)
        student_gpa = 3.8              # Kiểu số thực (float)
        is_active = True               # Kiểu luận lý (bool)
        print("--- THÔNG TIN SINH VIÊN ---")
        print(f"Họ tên: {student_name}")
        print(f"Tuổi: {student_age}")
        print(f"GPA: {student_gpa}")
        extra_years = input("Nhập số năm cộng thêm: ")
        future_age = student_age + int(extra_years)
        print(f"Tuổi của sinh viên sau {extra_years} năm nữa là: {future_age}")
  - Tránh bỏ sót các tham số bắt buộc.
  - Cấu hình môi trường ảo venv chính xác và không đặt trùng tên file hệ thống.
  [Prompt: Generate a flowchart diagram showing standard input processed by control flow logic and printing the formatted output.]
  ```

## Session 01: Ép kiểu dữ liệu f-string
  ```markmap
  - Hiểu rõ khái niệm cốt lõi và kiến trúc của Ép kiểu dữ liệu f-string.
  - Áp dụng các quy tắc triển khai thực tiễn để giải quyết bài toán: Ứng dụng f-string xuất dữ liệu đẹp.
  - Vận hành và kiểm tra đầu ra hoạt động ổn định.
  - Giải pháp kỹ thuật giúp tối ưu hóa hiệu năng, giảm thiểu blocking I/O và tự động hóa validation.
  - Ví dụ triển khai:
        student_name = "Nguyễn Văn A"  # Kiểu chuỗi (str)
        student_age = 20               # Kiểu số nguyên (int)
        student_gpa = 3.8              # Kiểu số thực (float)
        is_active = True               # Kiểu luận lý (bool)
        print("--- THÔNG TIN SINH VIÊN ---")
        print(f"Họ tên: {student_name}")
        print(f"Tuổi: {student_age}")
        print(f"GPA: {student_gpa}")
        extra_years = input("Nhập số năm cộng thêm: ")
        future_age = student_age + int(extra_years)
        print(f"Tuổi của sinh viên sau {extra_years} năm nữa là: {future_age}")
  - Tránh bỏ sót các tham số bắt buộc.
  - Cấu hình môi trường ảo venv chính xác và không đặt trùng tên file hệ thống.
  [Prompt: Generate a flowchart diagram showing standard input processed by control flow logic and printing the formatted output.]
  ```
